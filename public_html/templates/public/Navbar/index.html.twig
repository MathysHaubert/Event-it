<div class="body">
    <img src="assets/Resource/img/logo.png" alt="logo" class="images">
    <nav class="navbar">
        <ul class="nav-list">
            <li class="nav-item">
                <a href="{{ path('/') }}" class="nav-link">Acceuil</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('userlist') }}" class="nav-link">Liste des utilisateurs</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('event_it.public.faq') }}" class="nav-link">FAQ</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('contact') }}" class="nav-link">Contact</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('contact') }}" class="nav-link">{{ user }}</a>
            </li>

            {% if user.role is same as('ADMIN') %}
              <li class="nav-item">
                <a href="{{ path('contact') }}" class="nav-link">manage faq</a>
              </li>
            {% endif %}
        </ul>
    </nav>
    <a href="{% if is_logged_in %}{{ path('statPage') }}{% else %}{{ path('login') }}{% endif %}">
        <img src="assets/Resource/img/UserButton.png" alt="logo" class="images">
    </a>
</div>

<link rel="stylesheet" href="assets/Resource/css/Navbar/style.css">

<script type="module" src="assets/Resource/js/Navbar/script.js"></script>
